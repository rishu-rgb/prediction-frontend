import React from 'react';
import ReactDOM from 'react-dom/client';
import DisasterWarningSystem from './disaster-warning-system';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <DisasterWarningSystem />
  </React.StrictMode>
);
